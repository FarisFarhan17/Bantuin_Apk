import 'package:flutter/material.dart';

class CampaignStatistics extends StatelessWidget {
  const CampaignStatistics({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Verifikasi Kampanye statistik'),
      ),
      body: const Center(
        child: Text('Verifikasi Kampanye Page statistik'),
      ),
    );
  }
}
