import 'package:flutter/material.dart';

class DonationVerification extends StatelessWidget {
  const DonationVerification({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Verifikasi Donasi Barang'),
      ),
      body: const Center(
        child: Text('Verifikasi Donasi Barang Page'),
      ),
    );
  }
} 